<%=render "new" %>
<div class="posts">
	<% @posts.each do |post| %>  
	 	<%= render partial: 'post',locals: { post: post }%>
	 	<%# debugger%>
	    <%= link_to 'Reviews',new_post_review_path(post), class: 'btn btn-primary btn-xs '%>

	<% end %>
</div>