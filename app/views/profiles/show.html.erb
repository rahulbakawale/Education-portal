<%= image_tag "http://localhost:3000#{@profile.image}"%>
 <h2>First name: <%= @profile.first_name %><br></h2>
   <h2>Last name: <%= @profile.last_name %><br></h2>
    <h2>City: <%= @profile.city %><br>
    	<%if @profile.user.id == current_user.id%>
    <%= link_to "Edit", edit_profile_path, class: "btn btn-primary  btn-xs"%>
    <% 	end %>
